/*
触发顺序：

UPDATE:
	BEFORE INSERT
	BEFORE UPDATE
	AFTER UPDATE

INSERT:
	BEFORE INSERT
	AFTER INSERT
 */

USE sakila;

CREATE TRIGGER City_AD
AFTER DELETE
  ON City
FOR EACH ROW
  INSERT INTO city_backup (city_id, city)
  VALUES (OLD.city_id, OLD.city);

